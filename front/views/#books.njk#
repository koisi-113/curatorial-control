{% extends "base.njk" %}
{% block title %}本の一覧{% endblock %}
{% block script %}
{% endblock %}
{% block content %}
<h1 class="ml-5 fw-bold mb-10" style="color: orange">一覧表示</h1>
<div class="d-flex justify-content-end mb-3">
    <a href="/top" class="me-2 btn btn-success">トップページへ</a>
    <a href="/top/books/add" class="btn btn-success">本の追加</a>
</div>

<table class="table table-hover">
    <tbody>
    {% if posts and posts.length > 0 %}
    <thead>
        <tr>
          <th scope="col" class="col-1"></th>
          <th scope="col" class="col-5">著書名</th>
          <th scope="col" class="col-3">著者名</th>
          <th scope="col" class="col-3">貸出</th>
        </tr>
    </thead>
    {%for post in posts%}
    <tr
        onclick="window.location.href=`top/books/${post.id}`"
        data-bs-toggle="tooltip"
        title="{{post.name}}の詳細ページへ"
        style="cursor: pointer"
    >
    <th scope="row">{{loop.index}}</th>
        <td>{{post.name}}</td>
        <td>{{post.author}}</td>
        <td>{%if post.is_borrowing%} 貸出中 {%else%} 利用可能 {%endif%}</td>
    </tr>
    {%endfor%}
    {% else %}
        <p class="text-center text-muted">本がありません。</p>
    {% endif %}
    </tbody>
</table>
{% endblock %}